#!/bin/env python
import unittest

# import scripts.bs_direction_compose as compose


class BsDirectionComposeTest(unittest.TestCase):
    def test_update_candidates(self):
        # codes = compose.update_candidates()
        # self.assertTrue(len(codes) > 0)
        self.assertTrue(True)
